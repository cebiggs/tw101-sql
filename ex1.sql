CREATE TABLE person (
    id INTEGER PRIMARY KEY,
    first_name TEXT,
    last_name TEXT,
    age INTEGER
);

-- extra credit
-- case insensitivity
-- add extra fields

create table person_downcase (
    id integer primary key,
    first_name text,
    middle_name text,
    last_name text,
    weight float,
    age integer
);
